---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for ExpressRoute circuits against a real Azure subscription."
---

# CATTS — ExpressRoute integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/express-route/*.tftest.hcl`
