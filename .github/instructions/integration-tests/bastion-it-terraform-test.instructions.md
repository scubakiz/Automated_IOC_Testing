---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Azure Bastion against a real Azure subscription."
---

# CATTS — Bastion integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/bastion/*.tftest.hcl`
