---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Virtual Network Gateways against a real Azure subscription."
---

# CATTS — VNet Gateway integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/vnet-gateway/*.tftest.hcl`
