---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Azure Private DNS Resolver against a real Azure subscription."
---

# CATTS — Private DNS Resolver integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/private-dns-resolver/*.tftest.hcl`
