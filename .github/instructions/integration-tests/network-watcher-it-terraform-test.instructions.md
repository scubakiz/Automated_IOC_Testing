---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Network Watcher/flow logs against a real Azure subscription."
---

# CATTS — Network Watcher integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/network-watcher/*.tftest.hcl`
