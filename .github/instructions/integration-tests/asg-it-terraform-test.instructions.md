---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Application Security Groups against a real Azure subscription."
---

# CATTS — ASG integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/asg/*.tftest.hcl`
