---
description: "Use when generating Terraform Testing Framework integration tests (.tftest.hcl) for Azure Route Server against a real Azure subscription."
---

# CATTS — Route Server integration tests (Terraform Testing Framework only)

Apply tests only when unique naming per test run is supported.

Output:

- `<terraform_root>/tests/integration-tests/route-server/*.tftest.hcl`
